# Project Rules

## Git Commits

- **Never** include `Generated with [Devin]` footer or `Co-Authored-By: Devin` line in commit messages.
- Commit messages must contain only the subject line + body — no AI attribution footer.
- Use `git commit -F .commit-msg.txt` on Windows (PowerShell does not support heredoc).
